# dist/ 目录

此目录包含由 GitHub Actions 自动构建的单文件版本。

## 文件说明

- `host_nginx_web.py` - 自动构建的单文件版本（包含所有模块）

## 构建流程

1. 开发者修改 `web/` 目录下的模块代码
2. 提交到 GitHub 后，GitHub Actions 自动运行
3. 执行 `build.py` 将所有模块合并为单文件
4. 自动提交到此目录

## 注意

⚠️ 不要手动修改此目录下的文件，所有修改会被自动构建覆盖。
✅ 修改请在 `web/` 目录的源码模块中进行。
